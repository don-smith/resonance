# Backlog Agent and Skill

Owner: team

Adds agent interaction to the backlog.

- It has a skill to manage the backlog.
- Adds new chat input on all backlog/decision pages.
- Uses LangGraph DeepAgents SDK for skill and filesystem capabilities.
- Can define provider and model in package config.
- Need to figure model token management. Package-level `.env` file?

