# Git package

Owner: team

Shows active branch worktree status and maybe eventually actions for "stage all", and "commit" with message prompt.

Note: We may decide to make this a personal/member package instead of a team one.