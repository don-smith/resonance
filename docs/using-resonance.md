# Using resonance

Run `resonance` from the repository you want to inspect. Markdown files are discovered recursively, except inside `.git` and `node_modules`.

Select a document in the left navigation tree. Edit the source file in your editor, then reload the page to see the change.
